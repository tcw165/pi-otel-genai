node_modules/
tmp/
.DS_Store
PLAN.md
PROGRESS.md

# npm lock file (project uses pnpm — pnpm-lock.yaml is the lock file)
package-lock.json

# Bazel output / cache symlinks
bazel-*

# Distribution output (generated by `make dist`)
dist/
